# Changelog

All notable changes to this project are documented in this file.
Все значимые изменения проекта фиксируются в этом файле.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)
and this project aims to follow [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Added

- Initial changelog stub and release policy baseline (Step 024).
- Добавлен начальный changelog stub и базовая release policy (Step 024).

### Changed

- No runtime changes yet.
- Runtime-изменений пока нет.

## Release Tag Policy / Политика тегов

- Use annotated tags only: `vMAJOR.MINOR.PATCH` (for example: `v0.1.0`).
- Использовать только annotated tags формата `vMAJOR.MINOR.PATCH` (например: `v0.1.0`).
- Every release tag should map to a matching changelog section.
- Каждый релизный тег должен соответствовать секции в changelog.
